language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests
# pre-build step to set language
before_install:
  - sudo apt-get update && sudo apt-get --reinstall install -qq language-pack-en
